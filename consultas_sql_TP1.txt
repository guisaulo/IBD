a1:SELECT NOME,TELEFONE FROM MEDICO WHERE NOME IN (SELECT NOME FROM MEDICO WHERE NOME='SORAIA' UNION SELECT NOME FROM MEDICO WHERE NOME='PAULO');
a2:SELECT * FROM (MEDICO, MEDICO_ESPECIALIDADE), ESPECIALIDADE;
a3:SELECT M.NOME mNOME,C.NOME cNOME FROM MEDICO M JOIN MEDICO_CLINICA MC JOIN CLINICA C ON M.COD=MC.MEDICO_COD AND MC.CLINICA_COD=C.COD;
a4:SELECT M.NOME FROM MEDICO M JOIN MEDICO_CLINICA MC ON M.COD=MC.MEDICO_COD AND MC.CLINICA_COD=4 AND M.NOME NOT IN (SELECT M.NOME FROM MEDICO M JOIN MEDICO_CLINICA MC ON MC.CLINICA_COD=10 AND M.COD=MC.MEDICO_COD);
a5:SELECT M.NOME FROM MEDICO M JOIN MEDICO_CLINICA MC ON M.COD=MC.MEDICO_COD AND MC.CLINICA_COD=4 AND M.NOME IN (SELECT M.NOME FROM MEDICO M JOIN MEDICO_CLINICA MC ON MC.CLINICA_COD=10 AND M.COD=MC.MEDICO_COD);
a6:SELECT DISTINCT C1.MEDICO_COD FROM CONSULTA C1,CONSULTA C2,MEDICO M WHERE C1.MEDICO_COD=M.COD AND C1.COD=C2.COD;
b1:SELECT R2.NOME,C.NOME,CON.DATAHORA FROM (SELECT * FROM (SELECT * FROM MEDICO_ESPECIALIDADE JOIN MEDICO ON COD=MEDICO_COD) AS R1 GROUP BY COD HAVING COUNT(*)>1) AS R2, CLINICA C, MEDICO_CLINICA MC, CONSULTA CON WHERE C.COD=MC.CLINICA_COD AND R2.COD=MC.MEDICO_COD AND CON.MEDICO_COD=MC.MEDICO_COD ORDER BY R2.NOME ASC;
b6:SELECT M.NOME,COUNT(*) as numCONSULTAS FROM MEDICO M JOIN CONSULTA C ON M.COD=C.MEDICO_COD GROUP BY C.MEDICO_COD ORDER BY numCONSULTAS, M.NOME ASC;
b5:SELECT NOME FROM MEDICO JOIN MEDICO_ESPECIALIDADE ON COD=MEDICO_COD AND MEDICO_COD IN (SELECT MEDICO_COD FROM MEDICO_ESPECIALIDADE GROUP BY MEDICO_COD HAVING COUNT(*)>1) GROUP BY COD ORDER BY NOME ASC;
b9:SELECT C.NOME,C.ENDERECO FROM ESPECIALIDADE E JOIN CLINICA C JOIN ESPECIALIDADE_CLINICA EC ON E.COD=EC.ESPECIALIDADE_COD AND C.COD=EC.CLINICA_COD AND (E.NOME="DERMATOLOGIA" OR E.NOME="CARDIOLOGIA");
b8:SELECT E.NOME,COUNT(*) AS numMEDICOS FROM MEDICO_ESPECIALIDADE ME JOIN ESPECIALIDADE E JOIN MEDICO M ON E.COD=ME.ESPECIALIDADE_COD AND M.COD=ME.MEDICO_COD GROUP BY NOME HAVING COUNT(*) >= 0 ORDER BY numMEDICOS DESC,E.NOME ASC;
b7:SELECT NOME,TELEFONE FROM MEDICO WHERE TELEFONE LIKE "____-__11" OR TELEFONE LIKE "____-_11_" OR TELEFONE LIKE "____-11__" OR TELEFONE LIKE "___1-1___" OR TELEFONE LIKE "__11-____" OR TELEFONE LIKE "_11_-____" OR TELEFONE LIKE "11__-____" ORDER BY NOME ASC;
c1:SELECT M.NOME,E.NOME FROM MEDICO M,ESPECIALIDADE E,MEDICO_CLINICA MC,MEDICO_ESPECIALIDADE ME,CLINICA C WHERE M.COD=MC.MEDICO_COD AND MC.CLINICA_COD=C.COD AND C.ENDERECO="SAGRADA FAMILIA" AND ME.MEDICO_COD=M.COD AND ME.ESPECIALIDADE_COD=E.COD AND E.NOME<>"UROLOGIA" ORDER BY M.NOME ASC;
c2:SELECT M.NOME,E.NOME,COUNT(*) as CONSULTAS FROM CONSULTA C JOIN MEDICO M JOIN ESPECIALIDADE AS E ON M.COD=C.MEDICO_COD AND C.ESPECIALIDADE_COD=E.COD GROUP BY M.NOME,E.NOME HAVING COUNT(*)>=0 ORDER BY M.NOME ASC;
c3:SELECT MIN(ME.DATA_ESPECIALIZACAO),ME.ESPECIALIDADE_COD FROM MEDICO_ESPECIALIDADE ME JOIN MEDICO M ON M.COD = ME.MEDICO_COD AND ME.DATA_ESPECIALIZACAO <> '0000-00-00 00:00:00' GROUP BY ME.ESPECIALIDADE_COD ORDER BY MIN(ME.DATA_ESPECIALIZACAO) DESC;
c4:SELECT AVG(MC.SALARIO) as SalarioMedio, MIN(MC.SALARIO) as SalarioMin, MAX(MC.SALARIO) as SalarioMax, E.NOME FROM MEDICO_CLINICA MC JOIN MEDICO_ESPECIALIDADE ME JOIN ESPECIALIDADE E WHERE MC.MEDICO_COD=ME.MEDICO_COD AND ME.ESPECIALIDADE_COD=E.COD GROUP BY ME.ESPECIALIDADE_COD ORDER BY E.NOME ASC;
c5:SELECT * FROM MEDICO M1 JOIN MEDICO M2 ON M1.SOBRENOME=M2.SOBRENOME WHERE M1.COD>M2.COD ORDER BY M1.NOME ASC, M2.NOME ASC;
